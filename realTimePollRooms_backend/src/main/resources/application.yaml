spring:
  application:
    name: Polling-backend

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${PORT:8080}
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://root:AccaoqJZEzPDIHoLgkzEzREcXFbexbhr@crossover.proxy.rlwy.net:10614/railway
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format-sql: true

logging:
  level:
    org:
      springframework:
        web: INFO
    com:
      pollingapp:
        backend: DEBUG
---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


logging:
  level:
    org:
      springframework:
        web: INFO
    com:
      pollingapp:
        backend: INFO
